{
  "name": "redda",
  "dependencies": {},
  "scripts": {
    "test": "NODE_ENV=test jest --watch",
    "coverage": "NODE_ENV=test jest --coverage"
  },
  "transform": {
    "^.+\\.js?$": "babel-jest"
  },
  "devDependencies": {
    "jest": "^23.1.0"
  },
  "jest": {
    "verbose": true
  }
}
